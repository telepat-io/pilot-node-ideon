# Secrets — the ed25519 publisher signing key lives here. NEVER commit it.
/secure/

# Build outputs (regenerated by scripts/build-all.sh / scripts/build-free.sh)
/build/
/bundles/
/bundles-free/
/dist/

# Local deploy env (copy from .env.example)
.env

# Node
node_modules/
app/node_modules/
app/bin/
